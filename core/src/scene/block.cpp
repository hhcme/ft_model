#include "cad/scene/block.h"

namespace cad {

// Block is a plain data struct — no implementation needed beyond the header.
// Block bounds are computed externally by the scene when entities are added
// or modified, since the block needs access to its referenced entities.

} // namespace cad
